Write comprehensive unit tests for class: $ARGUMENTS

Tests conventions:
* Use unit tests with Mockito
* Use JUnit 5
* Use AssertJ
* Use annotations instead of static methods when possible
* Place tests in the same package as the class under test
